{
  "name": "idea-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run start  --parallel --ws ",
    "check:types": "npm run check:types --ws",
    "check": "npm run check --ws",
    "check:style": "npm run check:style -w @idea/client",
    "check:fix": "npm run check:fix --ws",
    "prepare": "husky"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "copy-files": "^0.1.0",
    "husky": "^9.1.7"
  }
}
